module;

#include <plf_hive.h>

export module hive;

namespace mo_yanxi{
	export using plf::hive;
}

namespace std{
	export using std::advance;
	export using std::next;
	export using std::prev;
	export using std::distance;
	export using std::swap;
	export using std::erase_if;
	export using std::erase;
}